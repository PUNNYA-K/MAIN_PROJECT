import 'package:flutter/material.dart';

class FavoritesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Favorites Page Content',
        style: TextStyle(fontFamily: 'Inder', fontSize: 18, color: Colors.black54),
      ),
    );
  }
}
